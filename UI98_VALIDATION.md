# UI98 验证记录

## 结论

**151 / 151 组离线测试返回成功。** 原有测试中的 4 个原生资源子测试跳过，原因均是当前环境没有相应 Dota 安装资源。
测试组成功不等同于原生客户端实测成功。

逐组命令、返回值、标准输出和错误输出见：

```text
tests/results/ui98-regression.json
```

完成布局间距的最后一次 CSS 调整后，又执行了 `tests/fantasy-ui.test.js` 和 `tests/hud-sidebar.test.js`，两者均通过。

## 已验证

- JavaScript：所有 16 个 Panorama 源文件通过 Node 语法检查。
- Lua：全部 117 个游戏 Lua 源文件通过加载语法检查；游戏 Lua 未因本次 UI 改版而修改。
- 条件编辑器：分类显示、原生隐藏状态、草稿保留、取消/应用、只读限制和保存回调。
- 草稿预览：仅读取副本，不调用保存读入器，不破坏动作选择器、指定对象选择器对当前条件的引用。
- 排行榜：同时渲染两张榜、独立状态、两边大号个人名次、默认折叠规则、名次间隔和个人行高亮。
- 结算生命周期：晚到响应、旧局响应、重连、重新开始、普通掉落与终局榜单共存。
- 数据真实性：沿用原服务端快照和排名，不自行排序，不制造前十名；保留毫秒精度和普通文本名称。
- 静态资源：布局 ID 唯一，主题样式最后载入，本地图标/贴图和 VTEX 文件对应，所有新增本地化键存在。
- 安装契约：UI-only 安装脚本列出需要编译的资源，检查产物和两份本地化；仅做静态契约验证。
- 外观辅助检查：使用真实 JS 测试面板树和实际本地贴图，进行浏览器近似布局检查。
  该检查只能辅助观察信息层级，不是原生 Panorama 渲染，也不作为 Dota 兼容性的证明。

## 测试环境

```text
Node.js 22.16.0
Python 3.13.5
TexLua / Lua 5.3
```

容器没有 Lua 5.1。三个现有测试使用旧版 `setfenv` API，因此通过
`tests/ui98-lua-bootstrap.lua` 提供仅用于离线测试的函数环境兼容层。
该文件不被游戏加载，也没有替换游戏逻辑。

实际全量测试入口为：

```text
python scripts/test-all.py
```

运行时通过 `LUA_BIN` 和 `PATH` 选择调用 TexLua + bootstrap 的命令包装器。
开发机器若装有原生 Lua 5.1，可直接使用原项目测试入口，不需要兼容层。

Linux 上还修正了一个测试文件的 CRLF/LF 读取兼容性，并给原有面板替身补上实际 Image 面板提供的 `SetImage` 方法。
这两处是测试环境兼容修正，没有删除业务断言或跳过界面回归。

## 跳过的原生资源子测试

以下各 1 个子测试由原测试套件按环境条件跳过：

- `tests/campaign-loot-data.test.py`
- `tests/enemy-equipment-data.test.py`
- `tests/late-campaign-balance.test.py`
- `tests/vmap.test.py`

它们依赖原生 Dota 安装资源、VPK 或外部原生地图来源；具体输出保留在 JSON 报告中。
没有把这些子测试标记成已运行通过。

## 未在此环境执行

没有启动 Dota 2、没有运行 Windows PowerShell 部署脚本、没有运行 Valve `resourcecompiler.exe`、
没有发布创意工坊，也没有对真实在线排行榜服务发起上传测试。

因此不声称「解压即为已经编译的成品」或「所有分辨率实机无问题」。
交付的是完成修改的项目源代码、完整美术资源、编译安装入口和测试记录。
最终客户端验收请按 `README_UI98.md` 的步骤进行。
