# 创意工坊地图约两分钟载入：2026-09-11 诊断

用户反馈进入上传到创意工坊的地图约需两分钟。只读核查发现启动预加载覆盖全部关卡，是明确的工作量和优先测量对象；现有证据不能把用户报告的全部 120 秒归因于某一个阶段。

`addon_game_mode.lua:221` 的 `Precache` 遍历整个 `levels.kv`，通过 `PrecacheUnitByNameSync` / `PrecacheItemByNameSync` 逐个预加载不同单位和物品。重复条目已去重，但没有限制为第一关。对本机订阅缓存 UI38 与开发目录 UI40 独立解析，均得到以下数量：

| 开场预加载类别 | 不同资源数 |
|---|---:|
| 30 关中的敌方英雄 | 60 |
| 30 关中的野怪 | 15 |
| 小精灵指挥官 | 1 |
| 敌方装备、经验卷轴及固定物品 | 61 |
| 单位与物品同步调用总数 | 137 |

这是脚本入口的静态调用数量，不是测出的磁盘读取次数或加载秒数。原生英雄与装备还引用本机 Dota 的模型、材质、粒子、技能及音效；上传包大小并不代表进入地图时引擎需要准备的全部资源。招募英雄已有按购买触发的异步预加载，全部敌方关卡仍在开场同步准备。`addoninfo.txt` 的 `MinimalPrecache` 为 0；没有测量该选项的额外引擎影响，也没有改动它。

下载与等待证据：

- 本机 `steamapps/workshop/content/570/3799645167/3799645167.vpk` 为 13,097,384 字节，约 12.49 MiB。
- Steam `content_log.txt:24415` 记录 23:15:29 开始下载，23:15:31 完成安装；实际传输约 1.28 MB，安装展开约 13.10 MB。包括前面的调度和连接，从 23:15:25 到完成约 6 秒。
- 同一日志 `:24444` 与 `workshop_log.txt:10466` 记录 23:28:32 请求、23:28:33 校验完成且无需更新。这些操作自身不构成两分钟延迟；不排除未记录到的其他连接／服务器阶段。
- 工坊包与开发版的 setup timeout、auto-launch delay、hero selection、strategy、showcase、pregame 均为 0，没有项目配置的开场两分钟等待。120 秒是每场战斗上限。

版本需要单独核实：本机已下载包中的运行标记为 `rpg-runtime-v38-20260911`，缓存 manifest 为 `133746454684170994`。上传日志后来在 23:40:20 成功提交另一 manifest `1368506194255505839`，但截至本次检查，没有对应的新缓存下载记录。不能据此声称工坊线上最新包仍是 UI38，也不能把本地已安装 UI40 当成本次实际运行版本。

没有取得用户这次完整的游戏载入计时：检查时未找到运行中的 Dota 进程，`game/dota/console.log` 停留在 2026-09-10 23:03。旧的本地 VConsole 捕获不作为本次工坊性能测量。提取的工坊脚本与数据留在 `C:/Users/harjeb/AppData/Local/Temp/rpg_workshop_load_3799645167`，已验证 VPK 条目 CRC。

后续优化记录在 beads **dota2_rpg-vyp**：先记录真实经过时间及实际运行构建，再将启动预载收敛到必要资源和第一关，后续关卡提前异步准备，准备完成后才生成单位。保留缺失资源保护、关卡／重开代次校验，并通过实机比较验证效果。尚未改变加载策略，也没有部署、启动、停止或重载游戏；本次只保存诊断和后续事项。
