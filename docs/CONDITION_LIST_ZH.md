# 当前游戏条件完整中文清单（技能能力限制更新）

本清单逐项对应当前“条件设置”菜单：**36 个使用条件、35 个目标筛选、14 个目标排序，共 85 个选项**。中文名称按当前游戏文本原样列出；实际口径与文字有差异的地方单独注明。目的地、施法方式等动作选项另列，不算入 85 项。

## 一、目前实现到哪一步

这些菜单项已有界面配置、序列化、服务端校验及战斗判断代码。本次加入按技能限制、条件冲突检查、状态控制和原生生命周期观察，离线检查 62 / 62 通过。**本次没有执行原生资源编译、安装或逐技能游戏内验证**。此前版本文档中的部署记录不能代替本次验收；验证边界见 [可靠性更新说明](../RELIABILITY_UPDATE_ZH.md)。

原版本删除的 13 个选项保持删除；保留其他条目的原编号。本次追加 U40～U43，不重用旧编号空缺。

游戏入口：选择英雄 → 选择行动规则 → 条件设置。每条规则最多 **4 个使用条件 + 4 个目标筛选 + 2 个排序优先级**。同一组内全部满足才通过；排序先比较第 1 项，再比较第 2 项。留空表示不增加限制。修改后点击“应用”才提交，取消不应用草稿。每位英雄最多 32 条规则。

### 参数怎么填

- **P**：百分比，填 0～100，例如 40 表示 40%，不要填 0.4 表示 40%。
- **N**：数值门槛；人数、次数、层数使用整数，生命值等按对应数值填写。`≥`、`≤` 包含相等，`<` 不包含相等。
- **R / D**：Dota 世界中的距离单位；距离计算使用水平二维距离，不是屏幕像素。
- **T**：秒数。
- **动作名称**：通常填技能内部名称；留空使用本条动作。动作历史记录下令，不表示成功命中或施法完成。
- **状态名称**：填准确的原生 modifier 内部名称，不是中文技能名称；可从已观察名称中选择；手填未验证名称需要高级确认，本版不提供完整的英雄专属状态配方。

## 二、使用条件：36 项

| 编号 | 游戏内中文名称 | 参数 | 实际含义 | 内部标识 |
|---|---|---|---|---|
| U01 | 总是 | 无 | 不添加额外使用门槛；仍要通过技能可用性和目标合法性检查。 | `always` |
| U02 | 自身生命百分比 ≤ | 百分比 P | 施法者当前生命占最大生命的百分比不超过阈值；最大值不可用或为 0 时不成立。 | `self_hp_pct_lte` |
| U03 | 自身生命百分比 ≥ | 百分比 P | 施法者当前生命占最大生命的百分比至少为阈值；最大值不可用或为 0 时不成立。 | `self_hp_pct_gte` |
| U04 | 自身魔法百分比 ≤ | 百分比 P | 施法者当前魔法占最大魔法的百分比不超过阈值；最大值不可用或为 0 时不成立。 | `self_mana_pct_lte` |
| U05 | 自身魔法百分比 ≥ | 百分比 P | 施法者当前魔法占最大魔法的百分比至少为阈值；最大值不可用或为 0 时不成立。 | `self_mana_pct_gte` |
| U06 | 存活友军数 ≥ | 数值 N | 当前观察到的存活友军总数达到阈值，包含施法者；不受此技能射程限制。 | `alive_ally_count_gte` |
| U07 | 存活敌人数 ≥ | 数值 N | 当前观察到的存活敌军总数达到阈值；不是附近人数。 | `alive_enemy_count_gte` |
| U08 | 存活敌人数 ≤ | 数值 N | 当前观察到的存活敌军总数不超过阈值；不是附近人数。 | `alive_enemy_count_lte` |
| U10 | 附近友军数 ≥ | 数值 N、半径 R | 以施法者为中心，半径内相对于该中心的存活友军数至少为阈值；不计中心本人。 | `nearby_allies_gte` |
| U11 | 附近敌人数 ≥ | 数值 N、半径 R | 以施法者为中心，半径内相对于该中心的存活敌人数至少为阈值；不计中心本人。 | `nearby_enemies_gte` |
| U12 | 半径内没有敌人 | 半径 R | 以施法者为中心，指定半径内观察到的存活敌人数量为 0。 | `no_enemy_within` |
| U13 | 战斗时间 ≥ | 秒数 T | 本场战斗已经过的时间至少为填写的秒数。 | `elapsed_gte` |
| U14 | 战斗时间 ≤ | 秒数 T | 本场战斗已经过的时间不超过填写的秒数。 | `elapsed_lte` |
| U15 | 自身最近受到伤害 | 秒数 T | 施法者在最近填写的秒数内有受伤记录。 | `self_recently_damaged` |
| U16 | 任意友军最近受到伤害 | 秒数 T | 除施法者外，任意友军在最近填写的秒数内有受伤记录；不保证是最终选中的友军。 | `any_ally_recently_damaged` |
| U21 | 距指定动作下令至少 | 秒数 T、英雄技能图标（默认当前动作） | 距所选英雄的该动作最近一次下令至少经过这些秒数；本场从未下令或该英雄已离场时不成立。 | `action_elapsed_gte` |
| U22 | 距指定动作下令至多 | 秒数 T、英雄技能图标（默认当前动作） | 距所选英雄的该动作最近一次下令至多经过这些秒数；本场从未下令或该英雄已离场时不成立。 | `action_elapsed_lte` |
| U23 | 本场动作使用次数 < | 数值 N、英雄技能图标（默认当前动作） | 本场所选英雄该动作的下令次数严格小于阈值；不是命中或施法完成次数。 | `action_use_count_lt` |
| U24 | 技能充能次数 ≥ | 数值 N、英雄技能图标（默认当前动作） | 所选英雄原生技能当前可用充能次数至少为阈值；不是冷却剩余秒数，也不是通用道具堆叠数量。 | `ability_charges_gte` |
| U27 | 自身具有指定状态 | 状态内部名称 | 施法者具有填写的原生 modifier；使用内部名称，非中文显示名。 | `self_has_modifier` |
| U28 | 自身没有指定状态 | 状态内部名称 | 施法者没有填写的原生 modifier；名称必须准确。 | `self_not_has_modifier` |
| U29 | 自身状态层数 ≥ | 状态内部名称、数值 N | 施法者指定状态的层数至少为阈值；状态不存在不按 0 层处理。 | `self_modifier_stacks_gte` |
| U30 | 自身状态层数至多 | 状态内部名称、数值 N | 施法者指定状态的层数不超过阈值；状态不存在不按 0 层处理。 | `self_modifier_stacks_lte` |
| U31 | 自身状态剩余秒数至少 | 状态内部名称、秒数 T | 施法者指定状态的剩余秒数至少为阈值；状态不存在不成立，永久状态按无限时长处理。 | `self_modifier_remaining_gte` |
| U32 | 自身状态剩余时间 ≤ | 状态内部名称、秒数 T | 施法者指定状态的剩余秒数不超过阈值；状态不存在不成立，永久状态按无限时长处理。 | `self_modifier_remaining_lte` |
| U33 | 被抓单位是敌方 | 无 | 小小投掷：检查抓取半径内最近可抓单位；不会跳过较近友军去抓远处敌人。 | `tiny_grab_is_enemy` |
| U34 | 被抓单位是友方 | 无 | 小小投掷：最近可抓单位与自己同阵营。 | `tiny_grab_is_ally` |
| U35 | 被抓单位是英雄 | 无 | 小小投掷：最近可抓单位是英雄；落点目标另用目标筛选。 | `tiny_grab_is_hero` |
| U36 | 被抓单位是小怪 | 无 | 小小投掷：最近可抓单位是小怪。 | `tiny_grab_is_creep` |
| U37 | 被抓单位生命百分比 ≤ | 百分比 P | 小小投掷：检查被抓单位的血量，与落点目标血量独立。 | `tiny_grab_hp_pct_lte` |
| U38 | 被抓单位生命百分比 ≥ | 百分比 P | 小小投掷：检查被抓单位的血量，与落点目标血量独立。 | `tiny_grab_hp_pct_gte` |
| U39 | 成功使用指定技能 / 装备后再使用（秒内，仅一次） | 技能/装备、秒数 T、可选施法者 | 指定行动成功执行后，在窗口内接续当前行动；当前行动成功后消费该次触发。 | `action_succeeded_after` |
| U40 | 当前持续施法时长 >= 秒 | 秒数 T、可选动作与施法者 | 使用原生持续施法开始时刻；自身只能用于对应释放，或观察另一施法者的持续施法。不是上次下令时间。 | `channel_elapsed_gte` |
| U41 | 当前持续施法时长 <= 秒 | 秒数 T、可选动作与施法者 | 同 U40；没有持续施法或观测缺失时不成立，不按 0 秒处理。 | `channel_elapsed_lte` |
| U42 | 观察到的技能生命周期阶段 | 阶段、可选动作与施法者 | IDLE / REQUESTED / CASTING / EXECUTED / CHANNELING / FINISHED / INTERRUPTED / ENDED / UNCONFIRMED。下令不代表原生成功执行。 | `action_phase_is` |
| U43 | 当前蓄力的对应释放动作可用 | 无 | 当前持续施法对应的结束动作须实际归属于施法者、已学习、可见且激活。 | `release_action_available` |

抓取候选按原生 `grab_radius` 查询后取最近单位；没有候选、最近距离并列、最近单位为肉山/远古单位或原生 API 无法查询时不通过这组条件。最终仍由原生投掷判定可执行性。

## 三、目标筛选：35 项

每项都作用于同一个候选目标。先选择敌方、友方或自身等目标范围；筛选不会让技能突破其原生敌我、目标类型或射程限制。

| 编号 | 游戏内中文名称 | 参数 | 实际含义 | 内部标识 |
|---|---|---|---|---|
| F01 | 目标生命百分比 ≤ | 百分比 P | 候选目标当前生命占最大生命的百分比不超过阈值；最大值不可用或为 0 时不成立。 | `hp_pct_lte` |
| F02 | 目标生命百分比 ≥ | 百分比 P | 候选目标当前生命占最大生命的百分比至少为阈值；最大值不可用或为 0 时不成立。 | `hp_pct_gte` |
| F03 | 目标魔法百分比 ≤ | 百分比 P | 候选目标当前魔法占最大魔法的百分比不超过阈值；最大值不可用或为 0 时不成立。 | `mana_pct_lte` |
| F04 | 目标魔法百分比 ≥ | 百分比 P | 候选目标当前魔法占最大魔法的百分比至少为阈值；最大值不可用或为 0 时不成立。 | `mana_pct_gte` |
| F05 | 目标生命值 ≤ | 数值 N | 候选目标当前生命绝对值不超过阈值。 | `health_lte` |
| F06 | 目标生命值 ≥ | 数值 N | 候选目标当前生命绝对值至少为阈值。 | `health_gte` |
| F07 | 目标已损失生命值 ≥ | 数值 N | 候选目标最大生命减当前生命的差值至少为阈值，使用绝对数值。 | `missing_health_gte` |
| F08 | 目标损失生命值至多 | 数值 N | 候选目标最大生命减当前生命的差值不超过阈值，使用绝对数值。 | `missing_health_lte` |
| F09 | 目标距离 ≤ | 距离 D | 施法者与候选目标的二维距离不超过阈值；这不会扩大技能原生射程。 | `distance_lte` |
| F10 | 目标距离 ≥ | 距离 D | 施法者与候选目标的二维距离至少为阈值；这不会扩大技能原生射程。 | `distance_gte` |
| F11 | 附近友军数 ≥ | 数值 N、半径 R | 以该候选目标为中心，半径内相对于该中心的存活友军数至少为阈值；不计中心本人。 | `nearby_allies_gte` |
| F12 | 附近敌人数 ≥ | 数值 N、半径 R | 以该候选目标为中心，半径内相对于该中心的存活敌人数至少为阈值；不计中心本人。 | `nearby_enemies_gte` |
| F13 | 排除自己 | 无 | 候选目标不能是施法者本人。 | `exclude_self` |
| F14 | 英雄 | 无 | 要求原生 IsRealHero 为真；不把普通单位一律当作英雄。 | `is_hero` |
| F15 | 召唤物 | 无 | 原生标记为召唤物，或能识别为其他参战单位的归属单位。与“非英雄”不是同一个判断。 | `is_summon` |
| F17 | 目标是幻象 | 无 | 要求原生标记为幻象。 | `is_illusion` |
| F19 | 目标归属于施法者 | 无 | 目标必须是可识别归属到施法者的其他单位；不包含施法者本人。 | `owned_by_self` |
| F20 | 正在施法（含前摇） | 无 | 目标处于施法前摇或持续施法；不是“技能不在冷却”或普通攻击前摇。 | `is_casting` |
| F21 | 正在持续施法 | 无 | 目标正在持续施法；不包含普通施法前摇。 | `is_channeling` |
| F22 | 受到控制 | 无 | 眩晕、缠绕、沉默、变羊任意一种原生状态成立；未枚举所有游戏控制效果。 | `is_controlled` |
| F23 | 眩晕 | 无 | 原生眩晕状态为真。 | `is_stunned` |
| F24 | 沉默 | 无 | 原生沉默状态为真。 | `is_silenced` |
| F25 | 缠绕 | 无 | 原生缠绕状态为真。 | `is_rooted` |
| F26 | 技能免疫 | 无 | 读取原生 IsMagicImmune 为真；与伤害减免、无敌分开。 | `is_spell_immune` |
| F27 | 没有技能免疫 | 无 | 读取原生 IsMagicImmune 为假；不会覆盖技能自身的合法目标限制。 | `not_spell_immune` |
| F30 | 目标有可驱散的增益 | 无 | 至少有一个原生标记为可驱散且不是减益的 modifier；不判断本条技能是否具备对应驱散能力。 | `has_dispellable_buff` |
| F31 | 目标有可驱散的减益 | 无 | 至少有一个原生标记为可驱散且是减益的 modifier；不判断本条技能是否具备对应驱散能力。 | `has_dispellable_debuff` |
| F32 | 目标最近受到伤害 | 秒数 T | 这个候选目标在最近填写的秒数内有受伤记录。 | `recently_damaged` |
| F33 | 目标具有指定状态 | 状态内部名称 | 候选目标具有填写的原生 modifier；使用内部名称，非中文显示名。 | `has_modifier` |
| F34 | 目标没有指定状态 | 状态内部名称 | 候选目标没有填写的原生 modifier；名称必须准确。 | `not_has_modifier` |
| F35 | 目标状态层数 ≥ | 状态内部名称、数值 N | 候选目标指定状态的层数至少为阈值；状态不存在不按 0 层处理。 | `modifier_stacks_gte` |
| F36 | 目标状态层数至多 | 状态内部名称、数值 N | 候选目标指定状态的层数不超过阈值；状态不存在不按 0 层处理。 | `modifier_stacks_lte` |
| F37 | 目标状态剩余秒数至少 | 状态内部名称、秒数 T | 候选目标指定状态的剩余秒数至少为阈值；状态不存在不成立，永久状态按无限时长处理。 | `modifier_remaining_gte` |
| F38 | 目标状态剩余时间 ≤ | 状态内部名称、秒数 T | 候选目标指定状态的剩余秒数不超过阈值；状态不存在不成立，永久状态按无限时长处理。 | `modifier_remaining_lte` |
| F39 | 指定敌方目标 | 当前敌方头像 | 仅匹配选定单位；死亡、离场或换关时整条规则跳过。同关重试保留对应位置。 | `specified_enemy` |

## 四、目标排序：14 项

排序不填参数，只决定合格候选目标之间的优先顺序，不代表最低血量等数值门槛。

| 编号 | 游戏内中文名称 | 参数 | 实际含义 | 内部标识 |
|---|---|---|---|---|
| P01 | 距离最近 | 无 | 优先选择距施法者二维距离最小的候选目标。 | `nearest` |
| P02 | 距离最远 | 无 | 优先选择距施法者二维距离最大的候选目标。 | `farthest` |
| P03 | 生命百分比最低 | 无 | 按当前生命 / 最大生命升序选择。 | `lowest_hp_pct` |
| P04 | 生命百分比最高 | 无 | 按当前生命 / 最大生命降序选择。 | `highest_hp_pct` |
| P05 | 生命值最低 | 无 | 按当前生命绝对值升序选择。 | `lowest_health` |
| P06 | 生命值最高 | 无 | 按当前生命绝对值降序选择。 | `highest_health` |
| P07 | 损失生命值最多 | 无 | 优先选择最大生命减当前生命最多的目标。 | `most_missing_health` |
| P08 | 护甲最低 | 无 | 按原生物理护甲值升序选择。 | `lowest_armor` |
| P09 | 护甲最高 | 无 | 按原生物理护甲值降序选择。 | `highest_armor` |
| P10 | 攻击力最低 | 无 | 按原生平均攻击伤害值升序选择；不是实时 DPS。 | `lowest_attack_damage` |
| P11 | 攻击力最高 | 无 | 按原生平均攻击伤害值降序选择；不是实时 DPS。 | `highest_attack_damage` |
| P12 | 魔法抗性最低 | 无 | 按原生魔法抗性值升序选择。 | `lowest_magic_resistance` |
| P13 | 魔法抗性最高 | 无 | 按原生魔法抗性值降序选择。 | `highest_magic_resistance` |
| P14 | 队友优先，无合法队友时选自身 | 无 | 合法队友排在自身前；组内按第二排序项选择。原生目标限制和筛选仍生效。 | `prefer_teammate` |

## 五、容易理解错的统计口径

### 人数与敌我关系

“存活友军数 / 敌人数”统计本场战斗观察列表，不限制距离。列表包含参战单位，以及通过原生搜索和归属关系识别到的其他存活单位；不能理解为整张地图的所有单位。存活友军总数包含施法者。

“附近人数”不计中心单位本人。使用条件的中心是施法者；目标筛选的中心是候选目标。**目标筛选中的敌我关系也相对于候选目标**：如果候选目标是敌人，“其附近友军”通常是其他敌方单位，“其附近敌人”通常是我方单位。

### 状态、伤害和动作记录

“任意友军最近受到伤害”排除施法者，但并不保证受伤的是最终选中目标。需要治疗受伤的那个人时，用目标筛选“目标最近受到伤害”，再结合目标生命门槛。

同名状态有多个实例时，层数或剩余时间当前取可读取实例中的最大值，不按施加者分别筛选。不存在的状态不会作为 0 层或 0 秒通过比较；永久状态按无限剩余时间处理。可驱散状态只读取原生 modifier 的增益/减益及可驱散标记，不判断本条技能能否实际移除它。

距上次动作下令的时间条件在没有历史记录时不成立。技能充能读取技能原生充能接口，不是任意英雄特殊资源，也不是普通物品堆叠数。

高级名称必须准确。例如错误的状态名称可能使“具有状态”永远不成立，使“没有状态”一直成立。部分原生观测不可用时，数值条件会拒绝通过，不能把没有观察数据视作正常的 0 值。

## 六、动作选项与基础施法支持

| 项目 | 当前行为 | 限制 |
|---|---|---|
| 目标模式 | 自动识别、对单位施法、对地点施法 | 必须是技能原生支持的模式，不能把纯单位技能改成任意地点技能。 |
| Toggle / Autocast 状态 | 按原生标志分别开放开启、关闭和魔法滞回策略 | 二者使用不同原生状态；例如 40% 开、20% 关，含最短保持和原生确认保护。 |
| 基础矢量施法 | 默认选原生射程内最近的合法目标，以目标为锚点，沿施法者到目标方向继续延长 150 单位定义第二点 | 第二点用于基本方向，不承诺实际长度、完美落点、弹射收益或第二次命中；无专用方向条件面板。 |
| 持续施法与对应释放 | 启动后保留保护；精确登记的对应结束动作可使用原生计时条件 | 必须匹配当前原生 channel 和父技能；并非所有非 channel 蓄力或多阶段机制都已适配。 |

技能命中人数条件已移除，旧规则中的 `min_aoe_hits` 会被忽略并在保存时清除。附近敌人数条件仍可使用，但它只统计指定观察半径内的单位，不估算技能命中人数。

条件全部通过后，仍要检查技能等级、可用状态、冷却、魔法、充能、控制状态、原生目标合法性等。“总是”不会跳过这些检查。无目标技能的目标筛选用于检查是否存在符合条件的单位，不会把原生无目标技能变成单位指向技能。

## 七、配置示例

| 目的 | 使用条件 | 目标筛选 | 目标排序 |
|---|---|---|---|
| 治疗低血量友军 | 总是，或自行增加魔法门槛 | 目标范围选友方；目标生命百分比 ≤ 70 | 生命百分比最低，然后距离最近 |
| 周围至少两个敌人时使用普通范围技能 | 附近敌人数 ≥ 2，填写合适半径 | 按技能原生目标类型设置 | 距离最近 |
| 尝试打断持续施法 | 总是 | 敌方；正在持续施法 | 距离最近；技能本身还必须能打断 |
| 对最近敌人进行基础矢量施法 | 总是 | 敌方；其余可留空 | 距离最近；使用技能原生矢量模式 |
| 仅在下令次数少于三次时考虑动作 | 本场动作使用次数 < 3，动作名称留空 | 按技能需要设置 | 按技能需要设置 |

这些是通用规则写法，不是逐英雄实战验收结果。“低血量或最近受伤”应写成两条规则；把两个条件写在同一条规则里表示二者必须同时满足。

## 八、来源和相关文档

本表以菜单定义逐项核对，未把服务端遗留但菜单没有开放的词缀、护盾等条件混入 85 项。也未把本地化文件里存在但菜单未使用的魔法百分比排序算入其中。

- [菜单定义](../content/dota_addons/dota2_rpg/panorama/scripts/custom_game/condition_catalog.js)
- [中文界面文本](../game/dota_addons/dota2_rpg/resource/addon_schinese.txt)
- [条件判断实现](../game/dota_addons/dota2_rpg/scripts/vscripts/tactics/condition_registry.lua)
- [战斗观察与统计口径](../game/dota_addons/dota2_rpg/scripts/vscripts/tactics/tactic_bridge.lua)
- [状态读取和附近人数](../game/dota_addons/dota2_rpg/scripts/vscripts/tactics/condition_context.lua)
- [目标排序与选择](../game/dota_addons/dota2_rpg/scripts/vscripts/tactics/target_selector.lua)
- [通用条件实现及部署证据](CONDITION_IMPLEMENTATION.md)
- [英雄独特机制登记](UNIQUE_SKILL_MECHANISMS.md)

本次新增内容及独特机制边界以 [可靠性更新说明](../RELIABILITY_UPDATE_ZH.md) 为准；历史独特机制登记不等于原生验收结果。
