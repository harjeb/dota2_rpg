# v20：统一规则条件编辑入口

日期：2026-09-09；运行标记 `rpg-runtime-v20-20260909`，界面版本 20。

规则行移除了外层条件选择、阈值输入、效果和目标选择、自动接近开关，以及相应表头、XML 模板、脚本事件和样式。条件编辑统一通过“条件设置”按钮进入；技能图标和新增、删除、排序按钮保留。行高由 130px 缩至 62px，滚动计算同步调整。

原来的自动接近选项移至详细设置的“目标在射程外时”，可选择“等待进入射程”或“自动接近目标”。读取已有规则的 forced 值，保存仍使用原有 range_only / allow_approach 协议。已有条件、目标筛选、优先级及技能身份保留，渲染、打开设置、切换英雄和开始战斗不再读取或同步旧阈值控件。敌方规则保持只读限制。

验证：

- 实际 HUD 脚本与 XML 的模拟回归覆盖外层控件不存在、详细设置编辑保存重开、自动接近开关、默认规则、英雄切换/重生、独立敌方规则和只读规则。
- 紧凑列表 3 行无需滚动，7 行溢出 54px；删除后滚动位置归零。动作菜单仍按视口和缩放定位。
- 保留完整 472 个预设变体与 78 个条件 ID 的既有验证。移除仅针对旧外层控件的检查，改为验证其不存在及完整设置入口。
- 修复清理代码时漏掉的开战自动收起面板局部变量；战斗状态切换回归通过。
- `python dota2_rpg_issue_fixes/tests/run_checks.py` 全部通过（84 个 Lua 语法检查及既有行为、UI、数据、安装检查）。`tests/verify-addon.ps1` 和 `git diff --check` 通过。

部署前完整备份至 `C:/Users/harjeb/AppData/Local/Temp/dota2_rpg_v20_caejxc17`，目录内含部署日志与 manifest.json。安装后 85 个源文件逐字节一致；编译 condition_catalog、panorama_rule_sync、rpg_demo_hud 三个 JS，以及 HUD CSS/XML，五个资源均成功。地图未重新编译，VPK 与部署前备份一致，SHA256 为 `981b81d32370ea9f5dfd22cefe5f3b433889ca4e045f4a30eeb50fb5d8cdac05`。

没有启动、关闭 Dota 或截图；实际显示效果需用户重载到界面版本 20 后确认。
