# UI43：掉落排除配方卷轴与肉山战旗

按用户要求，从关卡掉落池移除所有配方卷轴和 `item_roshans_banner`（肉山战旗）。生成器同时按 `item_recipe_` 前缀和原生 `ItemRecipe=1` 排除配方，后续重新生成不会恢复。此限制仅影响掉落，不禁止原版商店购买。

本轮从 UI42 可抽取池移除 79 个配方和 1 个战旗：351 → **271 个源条目**，实际发放名称 349 → **269 种**。保留 187 普通装备／散件／消耗品、49 当前中立装备、19 中立附魔、16 特殊物品。魔晶／福佑仍使用肉山可转交版本，其他肉山奖励不受影响。

每关最多三次判定及其概率保持不变；其他物品在缩小后的池中重新等权抽取。UI42 的彻底新局重开与 UI41 的分关预加载不改动。

验证：完整 `scripts/test-all.py` **76/76 通过**，`tests/verify-addon.ps1` 通过（可选外部模型来源检查未重跑），生成目录与运行时 Lua 表回归确认无任何配方和肉山战旗。未进行实机抽奖验收。

已安装 UI43，5 个源文件更新，127 个安装源文件逐字节一致；无须重新编译 Panorama 或地图，也未启动／关闭／重载游戏。备份：`C:/Users/harjeb/AppData/Local/Temp/dota2_rpg_ui43_backup_20260912_085907`。Workshop 使用者需上传并更新订阅包。已在旧局发出的物品不会被追溯删除。
