# v13：英雄技能选择、条件编辑与敌方战斗修复

关联 Beads：`dota2_rpg-779`。运行/UI 标记：`rpg-runtime-v13-20260908` / 界面版本 13。

## 条件与行动菜单

U21/U22 的动作参数改为从当前双方场上英雄的原生技能图标中选择；U23 次数和 U24 充能沿用同一选择器。保留“当前英雄的本条动作”选项。列表按阵营、英雄及阵容位置分组，包括可见被动技能；被动技能若没有下令历史，时间条件不会虚构一次施法。

跨英雄引用通过稳定的 `action_actor` 阵容键与原生 `action_id` 一起保存、校验、同步和恢复。同名敌人使用不同 occurrence 键。每次求值从当前阵容解析实体；单位离场时条件不成立。同一实体复活保留下令历史，重建出的新实体从零开始，避免实体索引复用串入上一英雄的历史。时间/次数依然记录下令，不等同于原生施法完成或命中。

条件参数栏、每个字段和文本输入增加明确宽度、最小宽度、高度、文字颜色及内边距；文本字段和图标字段各有合适宽度。高级动作区单独使用横向布局，避免在非横向父容器中滥用 `fill-parent-flow`。

行动菜单排除当前隐藏的内部能力、占位能力及被动技能；技能以后被原生状态公开时重新进入菜单。缺失原生本地化时显示可读名称，避免 `#DOTA_TOOLTIP...` token 泄漏。已有自定义规则不被批量删除。

规则同步模块在英雄离场或实体替换时清理该英雄的失败通知及未返回请求，晚到的失败回包不能复活旧通知。这修复了关卡已无地狱熊怪时仍显示其“规则未生效”的缓存路径；当前运行关卡表本就没有地狱熊怪。

## 敌方出装与技能

出装按 16 个英雄的定位分别编写，覆盖 107 个关卡英雄实例；保留各实例装备数量和关卡阶段。小黑走敏捷远程物理路线：前期鞋/怨灵系带，中期假腿/魔龙枪/夜叉，后期飓风长戟/幻影斧/蝴蝶/BKB 等。力量核心、法术先手及辅助各使用适合其定位的装备，而非按敌方位置轮换通用装备包。配置脚本为 `scripts/author-enemy-equipment.py`，同时维护 `levels_v07.json` 与运行时 `levels.kv` 的英雄装备部分；不重写其他关卡字段。原无效 ID `item_assault_cuirass` 修正为原生 `item_assault`。

装备实例化逐项隔离失败、检查空句柄，并按 KV 数字槽位顺序装备；单件失败不再中断其余装备和敌方初始化。

冰女原本套用 `ai_healer_protect`：该通用模板将 `ability_1` 视为友方技能，导致目标错误。敌方现在从已学习的可见原生主动技能生成施法规则，根据原生行为与阵营元数据区分敌方、友方、自身，保留 AI 模板的普攻优先级与追击方式。冰霜新星、冰封禁制指向敌方；极寒领域要求原生范围内存在敌人。

## 原生控制与闪退排查

动作检查、自定义适配器回调、直接发令、追近均先检查原生离场/命令限制/眩晕/冻结状态；缴械、沉默、物品禁用、缠绕按动作类型处理。缠绕阻止移动和追近，并对标记 `ROOT_DISABLES` 的技能/物品生效，不额外禁止普通原地施法。追击调度尊重发令失败，避免报出虚假的 `chase_started`。

没有用时间结界 modifier、阵营或英雄名称自行模拟冻结：虚空假面相关原生例外仍以引擎状态为准。离线测试只能证明这些路径正确检查状态，不能证明用户现场的时间结界表现已经消失。

22:07:39 的闪退已独立取证并保留 `dota2_rpg-5sh`：第 14→15 关切换后 `server.dll+0x169D0D1` 访问冲突，根因尚未确定。v13 增加 `RPGLifecycle` 换关实体日志，详见 [闪退诊断](CRASH_DIAGNOSIS_2026-09-08.md)。

## 验证边界

自动化检查覆盖 U21/U22 图标选择→Apply→序列化→重开、跨英雄/重复敌人/实体复用、失效通知和迟到回包、原生控制与追击失败、敌方装备生成/源运行表一致性、冰女实际点目标下令，以及换关日志生命周期顺序。完整检查、PowerShell addon 验证和目标 Panorama 编译结果见本次提交及部署记录。

完整检查 `python dota2_rpg_issue_fixes/tests/run_checks.py` 通过（72 个 Lua 文件语法、live/overlay 运行及行为检查、Panorama 与 Python 检查）；`tests/verify-addon.ps1` 和 `git diff --check` 通过。日志位于 Windows Temp 的 `dota2_rpg_v13_checks.log` 与 `dota2_rpg_v13_verify.log`。

v13 已安装到本机 Dota addon。部署前完整备份、部署日志和 SHA-256 清单位于 `C:/Users/harjeb/AppData/Local/Temp/dota2_rpg_v13_gfgs8iqz`。76 个源文件与安装目标逐字节一致；condition_catalog、panorama_rule_sync、rpg_demo_hud 三个 JS 与 HUD CSS 分别编译成功，均 0 failed。本次未重编地图；部署前后 VPK 完全一致，SHA-256 为 `2520d8601e23a179edc7354aeaa5bad3e26a3860ff7b9aca854d517b7b7e8216`。

本轮不启动或停止 Dota，不截图。游戏内文字实际显示、战斗效果和闪退是否复发仍需用户下一次自行运行的证据；自动化和资源编译成功不代替实机验收。实机验收跟踪于 `dota2_rpg-4ud`。
