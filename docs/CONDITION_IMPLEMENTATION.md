# 通用条件实现与使用

完整中文菜单清单见 [当前游戏条件完整中文清单](CONDITION_LIST_ZH.md)，逐项列出 25 个使用条件、34 个目标筛选和 13 个排序选项，并注明实际统计口径。

本轮实现通用条件功能，包括矢量施法和普通引导技能的基础释放。英雄独有资源、连招、形态、落点优化和特定释放流程只登记，不提供专用执行逻辑。当前通用推荐方案的保守统计由 [覆盖审计](CONDITION_COVERAGE.md) 自动生成，不把原生技能效果或简单下令等同于掌握全部机制。

## 条件分层

| 层次 | 作用 | 例子 |
| --- | --- | --- |
| 使用条件，最多 4 项 | 决定当前是否考虑这条动作，全部满足才继续 | 自身血量 ≤ 40%；附近敌人 ≥ 2；战斗经过 10 秒；当前技能剩余次数 ≥ 1 |
| 目标筛选，最多 4 项 | 对同一个候选目标逐项判断，全部满足才保留 | 友军血量 ≤ 70%；近期受伤；不是自身；正在施法 |
| 目标排序，最多 2 项 | 在合格候选者之间依次比较 | 先选血量百分比最低，再选最近 |
| 普通动作选项 | 控制原生单位、地点、无目标和开关动作 | 原生单位/地点分支；Toggle 或 Autocast 开启、关闭及魔法滞回策略 |

矢量技能使用相同的通用条件和筛选，默认选择原生射程内最近的合法目标，以其位置作为起点，沿施法者到目标的方向给出第二点；不增加专用条件或方向配置面板。第二点仅用于确定基本方向，不承诺技能的实际长度、落点收益或第二次命中。普通引导技能按通用条件启动，引导期间不会被后备普攻或其他规则抢断，交由技能原生逻辑结束。

使用条件包含生命、魔法、战场人数、附近人数、近期伤害、时间、动作次数/历史、技能充能、以及通用状态存在、层数、剩余时间。目标筛选包含资源、距离、附近人数、英雄/普通单位/召唤物/幻象、归属、施法与控制状态、免疫、可驱散状态及通用 modifier。高级状态字段由使用者明确配置，本版不内置任何英雄专属 modifier 配方。

同一条规则内部使用 AND。需要“低血量或刚受伤”时，分别建立两条规则。每位英雄最多 32 条；技能规则先于普通攻击检查，普通攻击作为后备动作。动作时间和次数记录成功下达的命令，不等同于技能命中、伤害或完成。

## 操作方法

1. 选择我方英雄，选择本条动作，再点击“条件设置”。
2. 普通技能可以选通用推荐方案，也可以直接填写使用条件、目标筛选和优先级。推荐方案只修改弹窗草稿。
3. 点击“应用”才提交；取消不改变已编辑规则。服务端拒绝时会显示原因，较旧请求的返回不会覆盖新结果。
4. 开关开启和关闭分别配置为两条规则，给开启、关闭阈值留出间隔，避免反复切换。

比如治疗友军，可以筛选“目标生命百分比 ≤ 70%”，按“最低生命百分比”排序；不必用“任意友军受伤”替代选中目标的状态。普通范围攻击可设附近敌人数作为使用条件。技能命中人数条件已移除，旧规则的 `min_aoe_hits` 不再影响保存和执行；附近敌人数只观察指定半径内的单位，不估算实际命中。

动作列表使用原生技能名称作为稳定标识，保留可配置的额外主动技能和多个大招。纯被动不作为施法动作。读取服务端规则、英雄重生、换位和切换头像不会自动提交默认规则覆盖原有设置。正常游戏中敌方规则仅供查看，工具模式可编辑实际敌方单位。

## 执行边界

执行前仍检查学习等级、原生可用状态、冷却、魔法、充能和控制状态；已有施法前摇和持续施法期间不会被其他规则抢断。单位目标使用原生目标约束，地点目标使用实际位置和原生距离。无目标技能的目标筛选检查是否存在匹配单位；地点技能按合法目标及排序选择落点，不估算技能命中人数。

不实现残岩准备/消费、卡尔元素组合、特定引导子动作及提前释放、树木施法、专属朝向准备、矢量落点优化、远端陷阱/分身目的地、节奏/形态调度或召唤单位控制。这些记录在 [独特机制登记](UNIQUE_SKILL_MECHANISMS.md)。普通条件可以用于具有复杂效果的技能，但不因此宣称自动掌握该技能全部机制。

## 版本 9 条件删减

按清单原编号删除 U09、U17、U18、U19、U20、U25、U26、F16、F18、F28、F29、F39、F40。菜单、条件注册、校验和推荐方案同步移除；标签筛选关联的旧 Boss/治疗者快捷菜单也移除。旧规则读取时清理这些条件，保留其余规则内容；新提交的已删除条件会被拒绝。其他清单编号保持不变，空号不复用。原生无敌及其他目标合法性检查保留。

版本 9 完整检查和 `verify-addon.ps1` 通过：62 个 Lua 文件语法检查、界面删除与旧草稿兼容、旧规则迁移（含稀疏规则槽位）、剩余执行功能及覆盖审计均通过。地图 19 项编译成功、0 失败，10 个 Panorama 资源分别编译成功，无 invalid property 错误。73 个安装源文件逐字节与仓库一致；地图包 SHA-256 为 `435ac447f299ef8a4c9fd39e936c73e22fb80c409ea32b76e27d8c48353968d7`。部署前备份和日志在 `C:/Users/harjeb/AppData/Local/Temp/dota2_rpg_conditions9_20260908_130159`。已有基础资源警告仍由 `dota2_rpg-xrx` 跟踪。未启动或停止 Dota，游戏内验证仍由 `dota2_rpg-jut` 跟踪。

## 验证与部署

自动回归覆盖界面实际 XML、条件 4/4/2 的序列化、Lua 验证和执行、取消/应用、规则持久性、敌方只读、目标筛选、冷却/施法保护及通用方案审计。完整检查入口为 `python dota2_rpg_issue_fixes/tests/run_checks.py` 和 `tests/verify-addon.ps1`。部署使用 `scripts/install-addon.ps1 -Compile`，界面标识为版本 9。

自动测试和资源编译不能替代游戏内逐技能验证。未启动或停止 Dota 客户端；不把静态覆盖率标为实机成功率。游戏内验收独立跟踪为 `dota2_rpg-jut`。

以下为版本 8 的历史检查记录。完整检查通过：62 个 Lua 文件语法检查、正式源码及修复覆盖包运行测试、通用条件/界面/商店回归、7 项覆盖审计，以及 VMAP 10 项通过、1 项可选外部来源检查跳过。`verify-addon.ps1` 通过。原生资源编译地图 19 项成功、0 项失败，10 个 Panorama 资源分别编译成功，没有 invalid property 错误。

已部署到本机 Dota 2 插件目录，73 个安装源文件逐字节与仓库一致。地图包为 3,123,798 字节，SHA-256：`2acc0c6a8469134dc369b027347de961535d2cacd6f073b131cce0e0d846db48`。部署前备份和检查日志位于 `C:/Users/harjeb/AppData/Local/Temp/dota2_rpg_generic8_20260908_114054`（`content`、`game`、`checks.log`、`compile.log`、`verify.log`）。编译器仍报告已有基础资源缺失：generic.vfx/dev 材质、soundevents_test、surfaceproperties_steamaudio、nav_hulls；相关环境问题由 `dota2_rpg-xrx` 跟踪，不宣称零警告。
