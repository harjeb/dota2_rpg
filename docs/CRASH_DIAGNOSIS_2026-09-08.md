# 2026-09-08 22:07:39 换关闪退取证

关联 Beads：`dota2_rpg-5sh`；用户要求检查是否由 RPG 地图导致。

## 当前判断

确认是在 RPG v12 运行中、第 14 关胜利后切换第 15 关时发生的原生服务端访问冲突。现有证据更指向换关时单位/技能生命周期这一排查方向，**尚不能把根因归结为地图地形、某个技能或某一行 Lua**。

本轮 U21/U22、敌方装备及原生控制修复当时仍只在仓库工作区，未部署。日志确认运行版本为 `rpg-runtime-v12-20260908`（21:40:55 加载）。本次取证没有启动/停止 Dota、截图或加载地图。

## 日志时间线（本地时间）

- 22:07:35：第 14 关战斗结束，`Result=radiant Stars=3 Gold=3390(+290) XpPool=900`。
- 22:07:38：第 15 关生成冰女、火枪、主宰、斧王、莉娜；日志确认 `Level 'ch15' spawned 5 enemy units`。
- 同秒：我方玛尔斯、火枪、沙王、虚空假面、凤凰重新生成，均为 14 级，随后 `Back to setup. Next level: ch15`。
- 22:07:39：日志最后出现 `{ hero npc_dota_hero_phoenix }`，随后为崩溃上报初始化消息。仅凭这段 hero 名称不能断言凤凰是故障源。

临近异常还有沙王对不可见目标发出原生命令 27 被拒绝、沙王头像资源未加载、主宰生成时 `attach_hitloc` 在空模型上查询失败。这些是独立可观察异常，当前没有证据把其中任一项与访问冲突直接相连。21:09 的临时 inspect 脚本 Lua 异常属于更早会话，不能作为本次崩溃原因。

## 两份转储一致的异常

| 项目 | 结果 |
| --- | --- |
| Steam dump | `crash_dota2.exe_20260908220739_1.dmp`，2,628,815 字节 |
| 引擎 dump | `dota2_2026_0908_220739_0_accessviolation.mdmp`，3,385,054 字节 |
| 异常 | `0xC0000005`，读取地址 `0xF50` |
| 故障模块及偏移 | `server.dll + 0x169D0D1` |
| 故障线程 | `33676` |
| 异常上下文 | `RCX=0xF40`、`RDI=0` |

直接解析 minidump 的异常流、模块流及 AMD64 CONTEXT，按模块基址计算偏移。异常上下文 RSP 附近的原始栈内包含 `server.dll+0xE158B2`、`server.dll+0x1695C4B`、`server.dll+0x1698DB9` 等地址；这只是原始栈扫描，**不是已解栈和符号化的调用链**。本机 PATH 无 cdb/WinDbg，且没有匹配的 Valve 私有符号，因此不能为这些偏移命名原生函数。

9 月 6 日红线事件的 dump 为 `particles.dll + 0x1AE04C`、读取地址 0。本次模块和偏移均不同，不能直接复用上次“地图材质/粒子”的候选解释。本次是在已运行约 27 分钟、通过多关之后换关发生，并非加载 VPK 时立即崩溃。

取证时安装 VPK 的修改时间为 21:40:46、大小 3,124,597 字节、SHA-256 `2520d8601e23a179edc7354aeaa5bad3e26a3860ff7b9aca854d517b7b7e8216`。它与此前 v12 部署保全的 20:46:18 版本（SHA-256 `9991b68f0612abc324ede5872a2702a90e9c6d12625f298ae0db1cec77b17462`）不同，说明其间地图包曾更新；现有取证不能确定更新来源，更不能据此确认或排除地图关联。v13 本次部署没有重编或替换该 VPK，部署前后逐字节一致。

## 代码排查入口与边界

`addon_game_mode.lua` 的结算回调在 3 秒后调用 `SpawnLevelEnemies` 和 `RespawnPlayerRoster`。后者直接 `RemoveSelf()` 旧英雄，再重建新实体。旧英雄可能仍有关联的原生技能、召唤物、持续效果或延时回调；应优先调查这一生命周期边界，尤其是本次阵容中的凤凰等有变身/持续施法的英雄。

目前尚未证实哪个旧实体仍被引用，也未做用户实机复现，故不以猜测为依据批量删除原生 modifier 或重写技能。v13 已添加 `RPGLifecycle` 换关诊断日志：重置开始/结束、旧单位删除前/后、新单位创建返回及准备完成；记录序号、时间、关卡、阶段、实体 ID、存活/持续施法/离场状态、当前技能和 modifier 名称，且删除后不再查询旧实体。日志只在单位生命周期操作时产生，不占原有 1,500 条战斗追踪额度。

下一次用户自行操作后，可通过缺失的 `remove_after`、`create_returned` 或 `ready` 配对标记缩小故障阶段；单独缺失标记仍不能证明某个原生效果是根因。若可获取匹配符号或调试器，再进一步定位上述 server.dll 偏移。新增日志模块的操作顺序、失效句柄及创建/删除失败测试已通过。

## 证据保全

原始 console.log、两份 dump 与 SHA-256 清单已复制到：

`C:/Users/harjeb/AppData/Local/Temp/dota2_rpg_crash_220739_cy_vtdv1`

原始文件分别来自 Steam `dumps`、Dota `game/bin/win64` 和 `game/dota/console.log`。转储不提交 Git；本记录保存可审查结论和不确定性。
