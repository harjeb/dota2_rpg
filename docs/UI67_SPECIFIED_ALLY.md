# UI67：F40 指定友方目标

条件设置 → 目标阵营选“友方” → 目标筛选选 **F40 指定友方目标** → 点击头像菜单选择当前上场的具体友方英雄（可选自己）。这不是仅按阵营筛选，也不是从全部英雄或替补库选名字。

- 仅该英雄身份符合本筛选。未选、死亡、离场、转为敌方、超出合法范围或原生目标检查失败时，本条规则跳过，绝不在本条规则里换其他友方。之后的其他规则与系统普通攻击仍按既有引擎顺序处理。
- UNIT_TARGET 原生施法指向选定英雄；POINT 施法使用其当前坐标，不使用施法者或任意其他队友位置。其他筛选、优先级、射程和原生技能阵营约束继续生效。
- 保存字段为 `specified_ally` + `target_actor`，沿用动作英雄稳定身份（例如 `npc_dota_hero_lion`），不保存实体索引。身份在当前同队上场 roster 中重新解析，因此换关、同英雄重建实体后仍指向该英雄；替补或已移除英雄不匹配。界面保留失效引用并提示，不自动清除或换人。
- 服务端只允许英雄身份格式、友方目标阵营以及当前同队 roster 成员。敌方 F39 身份不能冒充友方。清除菜单移除整项筛选。

F39 及既有编号不变。原来实际有 **35** 个目标筛选（编号存在历史空号），本次追加第 36 个，编号 F40；当前共 37 使用条件 / 36 目标筛选 / 14 优先级，87 个选项。早期删除报告中的 F39/F40 指历史删除项，不代表当前身份筛选被删除。

中文和英文版本标记均为 **UI67**。已安装 10 个变更源码文件并逐一比对一致，3 个 Panorama 脚本原生编译全部成功；已安装中英文版本文件均与源码一致。备份：`C:/Users/harjeb/AppData/Local/Temp/dota2-rpg-ui67-9qv5oa7q`。没有启动或停止游戏、执行游戏命令；离线测试不等同实机原生效果验收。

## 回归

全量离线回归 **103/103 通过**。报告：`C:/Users/harjeb/AppData/Local/Temp/dota2-rpg-ui67-regression.json`（未覆盖已有工作区报告）。

- `tests/specified-ally.test.lua`：真实规则服务、选择器与战术引擎；格式/阵营/当前 roster 校验、死亡/移除/转敌失败关闭、原生 UNIT_TARGET 和 POINT 下令、目标移动和实体重建、快照保存。
- `tests/condition-ui-v2.test.js`：实际 HUD/控件，F40 编号、同队上场头像、保存/重开、替补失效引用、服务端格式往返；保留 F39 和完整模板覆盖。
- `tests/specified-enemy.test.lua`：原有 F39 行为不变。
