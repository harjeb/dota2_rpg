# UI44：首关胜利卡住、完整后台预加载与单一结算框

## 本次实机日志证据

只读连接当前 VConsole（未发送命令），取得 `C:/Users/harjeb/AppData/Local/Temp/rpg_ui43_first_win.log`。运行标记明确为 UI43。日志第 2815–2836 行附近，第一关生成后后台 `Prefetch` 调用 `addon_game_mode.lua:268` 的 `RealTime()` 报 nil；第 6615 行第一关胜利已结算 `Gold=975(+75) XpPool=120`，第 6636–6656 行第二关 `Request` 在同一处再次报错，打断三秒后的切关准备。这是确定的脚本错误，不是玩家配置或掉落导致。

此前仅依据 DLL 注册字符串就认为 RealTime 在 Dota Lua VM 可用，是错误的；UI41 的 mock 没有覆盖实际适配器，漏掉了此问题。UI44 改为已在实际战斗使用的 `GameRules:GetGameTime()`，与 `SetContextThink` 同一时间轴。暂停游戏时超时计时同样暂停，日志里的队列耗时是游戏秒，不是现实墙钟秒。

开场同步预加载的日志也不再把缺少计时 API 表示成 `elapsed=0.000`；现在明确输出 `elapsed=unavailable`。没有可用墙钟采样时不宣称测得零耗时或完整加载提速比例。

## 进入地图后加载全部后续关卡

按用户最新要求，第一关生成后就把 **第二关到第三十关全部加入后台队列**。开场同步集合仍只有首关及必需资源，不会在启动 Precache 中恢复全量同步加载。

后台按加入顺序处理（即关卡顺序），同种单位／装备跨关卡只加载一次；每项启动之间至少经过一次 0.1 秒延迟调度。当前需要的关卡优先于后台任务，已发出的原生请求不会被强行取消。单项失败保留错误，用户进入相关关卡时可重试，不变成同步阻塞回退。重开继续复用已加载资源。

## 一次结算显示全部奖励

装备图标、经验和金币都放进同一个 `SettlementPanel`，共用一个确认按钮及三秒自动关闭计时器。关闭时整个奖励摘要一起消失，不再先关装备、露出另一个金币经验框。无物品的胜利、失败救济金币／物品也使用同一结构。

旧结算代次、重复结算事件和过期的关闭回调不能重新弹出已关闭摘要。终局的“再来一次”保持可见、可点击，不随奖励摘要关闭消失。

## 验证

完整 `scripts/test-all.py` **76/76 通过**，`tests/verify-addon.ps1` 通过（可选外部模型来源未重跑）。新增回归直接执行生产 InitGameMode 的缓存适配器并令 `RealTime=nil`，实际调用预加载、请求、延迟回调、第二关生成和 ready 广播；另覆盖全部后台队列顺序、共享资源、逐项让出执行、前台优先。Panorama 测试使用实际 XML 层级，验证奖励同框、一次关闭、无物品、救济、终局重开和旧事件隔离。

2026-09-12 09:33 已安装 UI44，更新 7 个源文件；HUD XML、JS、CSS 原生编译成功，127 个安装源文件逐字节一致，地图和载入画面资源哈希保留。备份：`C:/Users/harjeb/AppData/Local/Temp/dota2_rpg_ui44_backup_20260912_093327`。

本次有 UI43 错误的实机证据，但 UI44 的第二关切换／全量异步资源完成及实际界面像素仍需重新载入后验收。没有启动、关闭、重载游戏，也没有发出战斗操作。
